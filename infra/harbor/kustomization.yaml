apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
helmCharts:
  - name: harbor
    repo: https://helm.goharbor.io
    version: v1.18.3
    releaseName: harbor
    namespace: harbor
    includeCRDs: true
    valuesFile: values.yaml

    # valuesInline:


# resources:
#   - httproute.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: caddy-config
  namespace: documentation
data:
  Caddyfile: |
    :8080 {
        root * /usr/share/caddy
        
        try_files {path} {path}.html {path}/
        
        file_server
    }